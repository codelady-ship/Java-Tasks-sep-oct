/*
package tasklar;

public class Test {

    public static void recursion(String string, int index) {
       if(index==string.length())return;
       recursion(string,index+1);
        System.out.println(string.charAt(index);


    }

    public static void recursion(char[] stringChars, int index) {
        if (index != stringChars.length) {
            recursion(stringChars, index:index+1);
            System.out.println(stringChars[index]);
        }
    }
}
*/
